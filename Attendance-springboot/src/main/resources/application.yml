spring:
  application:
    name: Attendance Management System
  data:
    mongodb:
      uri: mongodb+srv://dhruvnariani:Dhruv12345@cluster0.kzgryf8.mongodb.net/attendance-DB?appName=Cluster0
      auto-index-creation: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  servlet:
    context-path: /api

app:
  jwtSecret: your_jwt_secret_key_change_this_in_production_must_be_at_least_256_bits_long
  jwtExpirationMs: 86400000 # 24 hours in milliseconds
  demo-data:
    enabled: true
  base-url: http://localhost:8080
  upload:
    dir: uploads/attendance-proofs

logging:
  level:
    root: INFO
    com.attendance: DEBUG